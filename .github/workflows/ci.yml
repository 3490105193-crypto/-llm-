name: CI

on:
  push:
  pull_request:

jobs:
  quality:
    name: Repository quality gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run AI quality gate
        shell: pwsh
        run: ./tools/ai-quality.ps1

